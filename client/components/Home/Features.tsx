import { HomePageProps } from '@coursefull/props';
import { motion, type Variants } from 'motion/react';

export default function Features({ featuresRef }: Partial<HomePageProps>) {
    return <div></div>;
}
