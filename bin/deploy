#!/bin/bash

function secure-run () {
    op run --env-file=secrets/deploy.env.tpl -- $1
}

secure-run docker compose down
cd client
secure-run pnpm build 
secure-run pnpm upload-assets
cd ..
secure-run docker compose up --build -d